# Project Decisions Ledger

> Append-only log of binding project decisions. Never rewrite existing entries.
> See constitution §0 for authority ranking.

## Decisions

| # | Date | Decision | Rationale | Author |
|---|------|----------|-----------|--------|
| 1 | 2026-06-04 | Adopt Spec Kit + Squad framework from briandenicola/template | Establish structured AI-driven delivery pipeline for making the project production-ready | Lead |
| 2 | 2026-06-04 | Use ruff for all Python linting/formatting | Single tool replaces black + isort + flake8; faster, consistent | Lead |
| 3 | 2026-06-04 | Target Azure Container Apps for deployment | Aligns with Azure OpenAI dependency; serverless scaling for demo/prod | Lead |
| 4 | 2026-06-08 | Hire Squad crew for Demo 1 (`001-morning-planning-outreach`): Rusty (Lead/Architect, Reviewer), Livingston (Backend .NET/Agent Framework), Linus (Frontend React 19/MUI v9), Basher (Platform/Terraform/CI-CD), Yen (QA/Test, Reviewer) + Scribe + Ralph. Cast from one universe; charters persisted under `.squad\agents\` (require_charter=true honored). | Stand up a specialist team mapped 1:1 to the plan.md `src\` layout so T001–T047 have clear single-owner accountability and reviewer gates per §17/§21. | Squad (Coordinator) for Brian Denicola |
| 5 | 2026-06-08 | Kick off C#/.NET 10 implementation of Demo 1 per constitution §0 Hierarchy of Authority and ADR-0002 (supersedes the stale Python/FastAPI `copilot-instructions.md`, flagged for T043). MVP slice = Phases 1→2→3 (T001–T021) + T047; DEMO path before LIVE within US1; all data over HTTP via mock-api (Principle II); LIVE/DEMO JSON-shape parity (Principle III). This run produced team + Squad artifacts + delegation plan only — NO production code. | Constitution v0.2.1 + ADR-0002 mandate the C#/.NET 10 + Microsoft Agent Framework + Foundry + ACA stack; sequencing the MVP first makes the morning brief demoable offline before LIVE/Foundry and deployment work. | Squad (Coordinator) for Brian Denicola |
| 6 | 2026-06-08 | T001 package-version substitutions (build-green): FluentAssertions pinned **7.2.2** (v8 went paid/commercial — Xceed license); **JsonSchema.Net 9.2.1** added as the draft-2020-12 contract validator; **Microsoft.Agents.AI.AzureAI 1.0.0-rc5** (no stable release; LIVE/Foundry path only, unreferenced until Phase 3); `global.json` pins 10.0.100 with `rollForward: latestFeature` (installed SDK is 10.0.204); **Vite 7.3.5 + plugin-react 5.2.0** instead of Vite 8 (Vite 8/rolldown emits a relative index.html error under the repo's Windows junction path). All other versions match plan.md. | Keep T001–T014 build/test GREEN against currently-available stable packages while preserving plan intent; substitutions are license/availability/toolchain-driven, not design changes. | Squad (Coordinator) for Brian Denicola |
| 7 | 2026-06-08 | Phase 3 / US1 (MVP) technical decisions: (a) **Outreach composite = 0.40·wallet + 0.30·engagement + 0.30·eventRelevance**, each component normalized to [0,1] and rounded to 4dp; baseline `outreach` = top-3 most-affected clients (by wallet) re-ranked by composite — deterministic/byte-stable (data-model.md weights; US2/T024 refines on this base). (b) **`orchestration-api.Tests` hosts the real `mock-api` in-memory via `extern alias MockApiHost`** so T013/T047 exercise the full composer→mock-api HTTP seam (Principle II/FR-002) without an external process and without the global `Program` clash. (c) **LIVE `AgentRunner` (T019) wired against Microsoft.Agents.AI + Microsoft.Agents.AI.AzureAI 1.0.0-rc5**; the whole solution compiles clean (no LIVE blocker), but rc5's `AzureAIProjectChatClientExtensions.CreateAIAgentAsync` is `[Obsolete]` ("use native `AIProjectClient.Agents` APIs") — suppressed (CS0618) and isolated in `CreateFoundryAgentAsync`; **flagged to revisit** when AzureAI GAs. DEMO acquires no credentials (FR-008): `AgentRunner` construction is side-effect free; only LIVE `RunAsync` builds `DefaultAzureCredential`. | Lock the MVP morning brief as demoable offline with LIVE/DEMO JSON-shape parity (Principle III) while keeping build+tests green; the rc5 obsolete-API surface is the only LIVE risk and is isolated + tracked, not a build breaker. | Squad (Coordinator) for Brian Denicola |
| 8 | 2026-06-08 | Default Foundry model to gpt-4o-mini (2024-07-18) with GlobalStandard SKU and capacity 10. All parameters exposed as Terraform variables (foundry_model, foundry_model_version, foundry_model_sku, foundry_model_capacity); users override via terraform.tfvars for production. | Constitution forbids hardcoding models unavailable in arbitrary regions or exceeding quota. gpt-4o-mini provides broad regional availability, quota-friendly profile, and production override path while keeping demo/dev deployments out-of-box with minimal quota footprint. | Basher |
| 9 | 2026-06-08 | CD workflow uses OpenID Connect (OIDC) federation for Azure authentication; no service principal secrets stored in GitHub. Workflow uses `azure/login@v2` with client-id, tenant-id, subscription-id as GitHub secrets; no client-secret parameter (authentication via OIDC trust relationship). | Constitution Principle IX (security) requires secrets never committed. OIDC federation eliminates stored credentials, issues short-lived tokens per run, provides full audit trail in Azure AD, and reduces attack surface vs. secrets/PATs. Setup: federated credential on service principal, repository configuration in Azure AD App Registration, Contributor role grant, and three GitHub secrets. | Basher |
| 10 | 2026-06-08 | Container App Environment uses consumption plan with platform-managed networking; no custom VNet/subnet. Platform handles VNet creation, subnet sizing, NSG rules. Internal ingress for APIs (not internet-accessible); external ingress only for ui-app. Key Vault and Foundry accessed via Azure backbone. | Simplicity over premature complexity. Consumption plan is cost-efficient, zero network config (no CIDR planning, subnet delegation, route tables), sufficient security for demo/dev, and supports documented upgrade path (VNet injection, private endpoints, workload profiles) via Terraform variables with no code rewrite. | Basher |
| 11 | 2026-06-08 | Container Apps created with placeholder images; Terraform ignores image changes via `lifecycle { ignore_changes = [template[0].container[0].image] }`; CD pipeline owns image lifecycle via `az containerapp update --image`. Solves chicken-and-egg: ACR created by Terraform but `az acr build` requires ACR to exist. | Separates concerns: Terraform owns infrastructure (scaling, networking, secrets), CD pipeline owns image lifecycle (tags, rollbacks, blue-green). First apply creates healthy infrastructure; apps show health failures until CD runs. Documented in build log and deployment checklist. Alternatives (wait for az acr build during apply, null resources, local-exec) break Terraform state management or add fragility. | Basher |
| 12 | 2026-06-08 | ui-app uses nginx template substitution (`/etc/nginx/templates/*.conf.template`) for runtime environment variable injection. ENV ORCHESTRATION_API_URL set in Dockerfile; nginx entrypoint auto-runs envsubst, substituting `${ORCHESTRATION_API_URL}` placeholders (env vars only; nginx `$` variables like `$host` remain untouched). Single image works across all environments. | Standard nginx Docker pattern: envsubst only substitutes environment variables, not nginx's own variables. Avoids build-time substitution (per-environment image rebuilds), client-side env loading (URL exposure), custom entrypoint scripts (reinvents nginx), or hardcoding (violates twelve-factor). Zero custom scripting; standard pattern documented in Dockerfile. | Basher |
