// Agent provisioner — stub (T002).
//
// In Phase 3 (T020) this console job idempotently registers the morning-brief
// agent version in Azure AI Foundry via PersistentAgentsClient + DefaultAzureCredential
// (mirrors the reference init_agents.py). For now it is a no-op placeholder so the
// solution builds and the project slot exists per plan.md Project Structure.

Console.WriteLine("agent-provisioner: stub. Foundry agent registration lands in Phase 3 (T020).");
return 0;
